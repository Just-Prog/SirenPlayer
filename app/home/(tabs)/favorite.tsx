import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function HomeFavoriteTabView() {
  return (
    <SafeAreaView>
      <View>
        <Text>TEST Favourite</Text>
      </View>
    </SafeAreaView>
  );
}
